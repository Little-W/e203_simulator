TARGET = main

SRCDIRS = ${C_SRC_DIR} src

INCDIRS = ${C_SRC_DIR} inc

COMMON_FLAGS := -O2

include $(SIM_ROOT_DIR)/deps/C/makefiles/Makefile.base

$(shell export PATH=$(SIM_ROOT_DIR)/deps/C/gcc/bin/:$PATH)
